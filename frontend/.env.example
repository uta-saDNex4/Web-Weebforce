# Public backend origin; empty uses the frontend's /api proxy.
NEXT_PUBLIC_API_BASE_URL=
# For local backend development. Docker overrides this to http://backend:8000.
BACKEND_INTERNAL_URL=http://localhost:8000
